#The script concerned with sending a file over an existing connection.

#!/bin/bash
#Check if the device is connected to a mesh network.
if [ ! -e ./assigned_ip ];then echo 'You must be connected to a DisMANET mesh network first'
else
  #Incomplete.
  #TODO
fi
